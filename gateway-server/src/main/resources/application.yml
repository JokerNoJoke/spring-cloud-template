server:
  port: 8080

spring:
  application:
    name: gateway-server
  cloud:
    consul:
      host: 127.0.0.1
      port: 8500
      discovery:
        prefer-ip-address: true
    gateway:
      discovery:
        locator:
          enabled: true
